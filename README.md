# 🕒 StudyTimer v1.0.0

**StudyTimer** 是一款专为学习设计的桌面时间记录与可视化工具。它不仅能帮助您精准记录每一分钟的学习时间，还能通过多维度的图表分析，助您复盘学习状态，提升专注力。

---

## ✨ 核心亮点

- **智能计时与跨天处理**：点击“开始”即刻专注，点击“结束”自动记录。支持凌晨跨天记录自动拆分，确保统计数据精准到每一天。
- **今日 24 小时深度复盘**：
  - **全天分布图**：直观展示 24 小时内的学习密度，背景自动区分白天与黑夜。
  - **分钟级下钻**：点击图表中的柱子，即可查看该小时内每 10 分钟细分段的学习分布。
  - **昨日/今日切换**：支持一键回溯昨天的学习情况。
- **多维统计分析**：支持折线图（趋势）、饼图（占比）、条形图（时长），复盘周、月、年的学习轨迹。
- **个性化壁纸与裁剪**：
  - **手动裁剪**：内置“截图式”裁剪器，可自由选取图片区域作为背景。
  - **透明度调节**：支持 0-100% 背景透明度调节，确保壁纸美观与文字清晰共存。
- **数据管理**：支持 JSON 格式的导出与导入，方便跨设备同步与备份。

---

## 🚀 快速开始

### 1. 环境准备
确保您的电脑已安装 Python 3.10+。

### 2. 安装依赖
在项目根目录下运行：
```bash
pip install PyQt6 matplotlib pandas
```

### 3. 运行程序
```bash
python main.py
```

---

## 📦 打包为可执行程序 (.exe)

如果您想将软件打包成一个独立的小程序，放在桌面直接点击使用，请使用以下命令（需先安装 `pip install pyinstaller`）：

```bash
pyinstaller --noconsole --onefile --add-data "models.py;." --add-data "main_ui.py;." --add-data "stats_widget.py;." --add-data "settings_widget.py;." --add-data "today_widget.py;." --name "StudyTimer" main.py
```

**参数解析：**
- `--noconsole`: 运行软件时不显示黑色命令行窗口。
- `--onefile`: 将所有模块打包成一个独立的 `.exe` 文件。
- `--add-data`: 将必要的子模块文件包含在程序包内。
- `--name "StudyTimer"`: 设置生成的程序名称。

---

## 🛠️ 技术架构

- **GUI 框架**: PyQt6 (原生、流畅、美观)
- **数据可视化**: Matplotlib (专业、精准)
- **数据处理**: Pandas (高效)
- **存储**: JSON (轻量、通用)

---

## 📝 版本说明

- **v1.0.0**: 
  - 修复了 Matplotlib 中文乱码问题。
  - 修复了 24 小时按钮数字遮挡问题。
  - 新增了昨日/今日数据切换功能。
  - 优化了壁纸透明度与裁剪交互。

---

祝您在 StudyTimer 的陪伴下，学习进步，效率倍增！💪
